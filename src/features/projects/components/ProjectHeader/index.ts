import { AddTagsHeader } from "@app/features/projects/components/ProjectHeader/AddTagsHeader";

import { CreateProjectHeader } from "./CreateProjectHeader";
import { ProjectsHeader } from "./ProjectsHeader";

export const ProjectHeader = {
  AddTags: AddTagsHeader,
  CreateProject: CreateProjectHeader,
  Projects: ProjectsHeader,
};
