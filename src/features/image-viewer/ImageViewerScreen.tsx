import { RootScreenProps } from "@app/core/navigation/routes";
import { Text, View } from "react-native";

export const ImageViewerScreen = (props: RootScreenProps<"imageViewer">) => {
  return (
    <View>
      <Text>Hoka</Text>
    </View>
  );
};
